{
  "name": "graphicon",
  "version": "0.3.0",
  "private": true,
  "description": "A browser-based vector editor with professional path editing and optional secure AI SVG generation.",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "check": "node ../verify_graphicon.mjs",
    "test:e2e": "playwright test",
    "test": "pnpm run check && pnpm run test:e2e"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "8.18.0"
  },
  "devDependencies": {
    "@playwright/test": "1.55.0"
  }
}
